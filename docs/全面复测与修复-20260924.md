# 全面复测与修复记录 · 2026-09-24

本记录更新到北京时间 21:22。结论：**已修复本轮复现的代码缺陷，自动化回归通过；真实 AI 完整验收仍未通过，当前阻断是百宝箱 HTTP 503。** 不把错误提示、部分报告或未执行步骤计为成功。

21:44 补充：已进一步定位为网关 `Agent not found`，并定向重试 7 项 AI 功能，仍全部失败。后续诊断及恢复步骤见[百宝箱 503 排查记录](百宝箱503排查-20260924.md)。

## 1. 验收范围与环境

- 工作区：`D:\my\star-career-agent-zhejiang2026`；分支 `feat/workplace-training`；基线 `2703ae0`。
- 操作前及收尾已 fetch；最后检查 HEAD 与 `origin/develop` 提交差为 `0 / 0`。中途一次 GitHub 连接重置，随后重试成功。
- JDK 17.0.20.1、Node.js 24.15.0、Python 3.13、MySQL 8；数据库 `youthpath`，本机端口 3307。
- 前端 `http://localhost:5173`；后端 `http://127.0.0.1:8080/api`；41 张表、9,958 条岗位种子记录。
- 使用虚构账号、独立临时岗位和图片，不操作既有用户资料。未发邮件、未推送、未建 PR；没有运行远端 CI。

## 2. 测试结果

| 检查 | 结果 | 说明 |
|---|---|---|
| 后端 Maven 测试 | **46 / 46 通过**，0 失败、0 错误、0 跳过 | 连接本地 MySQL，包含新增权限、非法问卷和图片内容检查 |
| 前端 Node 测试 | **9 / 9 通过** | SSE 分片、错误传播、训练 HTTP 状态、鉴权头 |
| 管理脚本测试 | **5 / 5 通过** | 已有库保护、增量迁移失败时中止启动 |
| 前端生产构建 | 通过 | 仍有共享包大小告警，约 1.54 MB / gzip 526 KB |
| `npm audit` | **0 个已知漏洞** | 包括开发依赖；不是“软件绝对无漏洞”的承诺 |
| 真实接口主流程 | **9 / 12 通过** | 普通 AI 对话、六智能体报告 / PDF、AI 能力分析失败 |
| 真实接口扩展 | **26 / 27 通过** | 岗位 AI 对比失败；权限、图片、分页、训练故障状态等通过 |
| 真实训练完整流程 | 未通过 | 登录及鉴权两组检查通过；首轮生成返回 `PLATFORM_UNAVAILABLE`，后续生成 / 评分 / 回写未执行 |
| SMTP | TLS 和认证通过 | 未发送验证码，不能视作注册 / 找回邮件投递已验收 |
| 并发读取 | 20 次请求、5 并发通过 | 仅轻量读取检查，不是生产压测 |

后端 46 项组成：权限及问卷 9、图片 4、对话流 8、训练持久化 8、训练协议 8、训练工作台 8、岗位分页 1。

本轮主流程包括登录、画像增改读、问卷评分落库、岗位详情 / 图谱、成长任务与完成记录、Word 简历及权限隔离。图谱返回中心节点但无路径的岗位按实际数据处理，不虚构晋升或换岗路线。

扩展流程覆盖错误密码、本人修改密码并恢复、匿名及无效令牌、分页边界、不存在岗位、跨账号画像 / 任务写入、能力分数 / 历史 / 简历读取、图片上传查询删除、空文件及非图片拒绝、匿名岗位修改删除，以及三个训练模板的幂等创建、归属、草稿、版本冲突和取消。

## 3. 已修复的问题

| 问题 | 修复及验证 |
|---|---|
| 旧档案、成长、能力、聊天历史、简历等接口可跨账号访问 | 新增统一 `AccessGuard`；按当前登录主体校验对象归属，拒绝替换用户 ID；混合批量操作在修改前校验全部对象。真实双账号检查与 MockMvc 回归通过 |
| 匿名可修改 / 删除岗位，旧辅助接口放行 | 知识库读取仍可公开；岗位写操作、分析写入、旧用户范围删除和辅助接口要求数据库中的管理员角色。学生伪造令牌角色字段也不能获得权限；管理员维护临时岗位通过 |
| JWT 使用固定签名常量 | 改为后端 `.env` 的 `JWT_SECRET`，至少 32 字节；本机已生成随机值。请求同时检查数据库账号状态及角色，禁用 / 删除账号令牌不可复用 |
| 异步报告任务缺少持久化归属 | 新增 `career_report_job` 和迁移 012；状态查询及取消先校验所有者。迁移在真实启动中执行成功 |
| 权限异常被通用异常处理转成 HTTP 200 | 单独处理 `ResponseStatusException`，保持 400 / 401 / 403 / 404 等 HTTP 状态 |
| 非图片文件伪装为 PNG 也可上传 | 检查真实 PNG / JPEG 内容、5 MB 文件限制和 1600 万像素限制；解码后重新编码，移除尾部附加内容；不信任 MIME 或扩展名；API 不再返回服务器文件路径 |
| 空问卷 / 重复题目 / 非法选项可能覆盖能力分数 | 验证六个维度、题目唯一性、选项范围及每维 1–2 题；非法请求返回 400，既有能力记录保持不变 |
| 平台报错被当作正常 AI 回答保存 | 后端发送结构化错误事件；不保存失败或部分回复为完整 AI 消息。回归测试覆盖普通及图片分支；真实故障调用后数据库 AI 回复新增数为 0 |
| 岗位对比遇到业务失败一直加载 | 成功 / 失败均结束加载，显示明确错误与重试；输出文本先转义；雷达图使用岗位原始权重，去掉伪造的个人能力值 |
| 职业星图在窄窗口裁切 | 画布跟随实际可用高度，监听容器尺寸变化；窄屏导航横排，中心标签换行，底部图例可见；无路线时显示数据缺失提示 |
| 星图生成使用了错误类型的岗位 ID | 管理页同时传原始岗位 ID 与画像 ID；生成使用原始 ID，完成后读取新画像再刷新；普通用户不能触发写入 |
| 前端依赖存在已知漏洞 | 更新 Axios 1.20.0、ECharts 6.1.0 及锁文件中可修复依赖；全量 npm 审计为 0，测试与构建通过 |

报告调用失败对用户显示简短提示，技术异常保留在后端日志。旧报告任务若没有归属登记不能直接续接，需要重新发起。

## 4. 浏览器复查

- 使用本轮虚构账号通过真实 RSA / AES 登录。
- 普通用户直接访问 `/admin/job-info` 被重定向到个人中心；后端同时拒绝写入，不能靠修改前端绕过。
- `/compare` 在平台报错后显示“AI 分析暂不可用”及“重试分析”，不再永久显示扫描中。
- 普通对话明确显示 AI 服务暂不可用；刷新历史不会把故障内容当成正常 AI 答案。
- 多智能体页面可滚动：默认窗口滚动区域高度 524、内容高度约 877，滚动位置到约 353，第六个智能体可查看；生成失败后可重新发起。
- 三类训练入口和历史列表正常。AI 辅助办公失败后显示“重试本次任务”；回答草稿和作品摘要在切页返回、刷新后恢复。后端重启后，已存在的失败会话可重新加载。
- 星图在默认约 647×564 及 390×844 尺寸检查画布边界，390 宽窗口的页面宽度仍为 390；中心岗位和图例可显示，无路线数据有明确提示。
- 完成后退出临时账号，并撤销浏览器尺寸覆盖。

这些检查验证的是故障处理、保存与恢复。由于 AI 服务无法产生正常输出，**不能据此认定正常生成中切页、重启恢复、多智能体完整接续和训练评分已通过本轮测试**。

## 5. 当前外部阻断与未覆盖项

21:20 再次直接检查配置的平台地址，仍为 HTTP 503；真实应用调用也失败。因此下列链路仍需恢复平台后重新验收：普通对话、AI 能力分析、岗位对比、六段报告完成 / 自动成长目标 / 两种 PDF 导出、三类训练完整对话 / 作品版本 / 评分 / 能力回写 / 成长任务。

需要小组核实百宝箱应用是否运行、应用地址是否变化；密钥只在本地 `.env` 填写。未擅自替换平台、伪造输出或绕过平台鉴权。

其他边界：真实邮箱注册及找回投递、图片对话成功生成、全部后台写接口、生产 Nginx、长时间负载、多实例及生成中重启尚未完整验证。报告完成去重部分状态仍在进程内。导师大屏为静态演示，简历上传解析、报告分享和若干历史匹配接口仍是预留能力；详见 README。

## 6. 配置与数据清理

- 后端配置：`D:\my\star-career-agent-zhejiang2026\后端\.env`。
- 前端公钥：`D:\my\star-career-agent-zhejiang2026\前端\.env.local`。
- RSA 公私钥与前端公钥匹配；JWT 新密钥已配置，本次更新后应重新登录。
- 两个配置文件均被 Git 忽略；对本地变更文件扫描真实敏感配置值，未发现命中。
- 本轮虚构账号、训练会话、关联数据、临时图片和岗位已按精确 ID 清理。三个测试账号前缀检查均为 0；临时凭据元数据已删除；岗位总数仍为 9,958。
- 已保留前后端运行；MySQL 3307 正常。Nginx 未启动，本轮没有验证生产代理。

## 7. 可复现命令与本地证据

命令与依赖条件见 [README](../README.md#7-测试和验收)。后端集成测试前先停后端；真实平台脚本需要重新启动。扩展脚本依赖主验收保留的虚构账号，用完必须执行 `--cleanup`。

```powershell
python scripts/test_manage.py
python manage.py stop backend
python scripts/run_backend_tests.py
python manage.py start backend
npm --prefix 前端 test
npm --prefix 前端 run build
npm --prefix 前端 audit

python scripts/project_acceptance.py --keep-fixtures
python scripts/project_extended_acceptance.py
python scripts/project_acceptance.py --cleanup
python scripts/training_smoke.py
```

原始结果在 Git 忽略的 `logs/`，不要上传日志或配置：

- `project-acceptance-tests.log`：后端 46 项最终结果。
- `project-acceptance-result.json`：主流程 9 通过 / 3 失败。
- `project-extended-acceptance-result.json`：扩展 26 通过 / 1 失败。
- `training-smoke-result.json`：首轮训练平台错误与已执行步骤。
- `npm-audit-after-fix.json`：依赖审计。
- `final-configuration-check.json`：无密钥值的配置一致性和平台状态。
- `final-cleanup-check.json`：测试账号清理及数据计数。

后续上传仍须在功能分支按小组规范提交并 PR 到 `develop`，由 CI 和评审确认；本轮未执行上传。
