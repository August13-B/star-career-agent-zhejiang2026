# 开发过程问题汇总与修复记录

> 记录本次开发（国赛 A13 → 省赛 A02 适配）中遇到并修复的**全部问题**
> 更新时间：2026-08-15 ｜ 共 **24 个 PR**、**4 个数据库迁移**

---

## 一、环境与启动类

| # | 问题现象 | 根本原因 | 修复 | PR |
|---|---|---|---|---|
| 1 | 后端启动失败：`The alias 'JobHardRequirement' is already mapped` | 合并后两个包存在 8 个同名 Job* 实体，`type-aliases-package` 同时注册 | 删除业务服务遗留 JobProfile 模块 + 8 个重复实体 | #45 |
| 2 | 修完①又报 `InvalidKeyException: Missing key encoding` | Spring Boot 不原生读 `.env`，直接 `mvnw spring-boot:run` 时 RSA 密钥为空 | `application.yml` 加 `spring.config.import: optional:file:./.env[.properties]` | #45 |
| 3 | `Web server failed to start. Port 8080 was already in use` | 旧后端进程未关闭 | 新增 `_pid_on_port()` + 启动前检测 + **`python manage.py free-port backend`** 命令 | #65 |
| 4 | 前端报 `'vite' 不是内部或外部命令` | `node_modules` 未安装 | `manage.py` 启动前端前自动 `npm install`；package.json 变更后自动重装 | #47 |
| 5 | 前端报 `Failed to resolve import "vue-router"` | 原项目 `package.json` 依赖清单不全（4 个包未声明） | 补齐 `vue-router` / `echarts` / `jsencrypt` / `crypto-js`（锁版本） | #48 |
| 6 | Vite 报 `http proxy error ... ECONNREFUSED 127.0.0.1:8080`（页面"无请求、无报错"） | ① 后端未启动；② 代理 target 写 `localhost` 在混合环境下可能解析到 **IPv6 `::1`**（实测 `[::1]:8080` 不可达） | 代理改 `http://127.0.0.1:8080`；`manage.py` 启动前端前检查后端端口并告警 | #61 #62 |

---

## 二、数据库与灌库类

| # | 问题现象 | 根本原因 | 修复 | PR |
|---|---|---|---|---|
| 7 | 灌库脚本导入报 `ERROR 1046: No database selected` | 脚本无 `CREATE DATABASE` / `USE` | 脚本顶部补 `CREATE DATABASE IF NOT EXISTS youthpath` + `USE` | #50 |
| 8 | 新环境灌库后岗位列表/星图全空 | 仓库只有表结构（0 条数据），9958 条岗位等数据未随仓库分发 | 导出 `数据库数据.sql`(24MB) + 向量数据(10139 条, gzip 60MB) 入库 | #51 |
| 9 | 每次手工灌库繁琐 | — | `manage.py` 幂等自动灌库（缺表建表 / 无数据导入 / 有数据跳过），新增 `db` 命令 | #51 |
| 10 | 画像字段解密失败 `Given final block not properly padded` | 库中密文由**另一套密钥**写入（与 `.env` 密钥不匹配） | 清理旧数据（迁移 002，保留岗位/画像/邀请码/账号），重录即用新密钥 | #58 |
| 11 | 导入数据报 `Unknown command '\"'`（静默失败） | 导入**必须**带 `--default-character-set=utf8mb4` | 写入文档；`manage.py` 内置该参数 | #51 |

---

## 三、画像 / 能力保存链路（本次最难的一串，4 个真 Bug 叠加）

> 现象：个人中心保存画像长期失败——「新增失败：新增学生信息失败 / 新增能力失败 / 网络错误 / 外键失败」

| # | 问题现象 | 根本原因 | 修复 | PR |
|---|---|---|---|---|
| 12 | 保存报「新增失败：新增能力失败」 | 能力/画像 Mapper 使用**多语句 SQL**（`insert ...; select ...;`），而 JDBC 连接串**未开 `allowMultiQueries`** | `.env`/`.env.example`/`application.yml` 连接串加 `allowMultiQueries=true` | #59 |
| 13 | 保存报 `Data too long for column 'expected_salary'` | 后端 RSA 加密后密文约 172 字符，而 `expected_salary` 列仅 `varchar(50)`、`education` 仅 `varchar(32)` | 迁移 **003**：6 个加密列加宽至 `varchar(1000)` | #59 |
| 14 | 保存成功却查不出来（报告读不到画像） | 插入未写 `is_deleted` → 值为 NULL，而查询条件是 `is_deleted = 0` | 迁移 **004**：NULL 行补齐为 0 | #59 |
| 15 | 画像字段仍是密文（"无法解析"） | 画像字段由 `rsaEncrypt` 写入，代码却用 AES 的 `decryptFromDB` 解；且查询用 `selectByCondition` 不命中 | 改用 `rsaDecrypt`（AES 兜底）+ Mapper 改 `selectByUserId` | #59 |
| 16 | 已有记录时保存报「新增失败」 | `student_ability.user_id` 有唯一约束，而前端 `myAbility.id` 为空时仍走 `insert` | 前端失败后**重新拉取并自动改用 update 重试**（3 处保存函数） | #60 |
| 17 | 保存报 `网络错误`（500） | 实体 6 个字段声明为 `Integer`，但 DB 列是 `varchar`、前端提交字符串（如 `gender="男"`）→ Jackson 反序列化失败 | 实体 6 字段改 `String`（gender/age/profileStatus/workTypePreference/studentGroup/privacyLevel）；性别展示抽 `genderText()` 兼容 `男/女` 与 `1/2` | #63 |
| 18 | 保存报 `字段 'user_name' doesn't have a default value` | 前端表单未填姓名（该列 NOT NULL） | 前端校验「姓名必填」（提示已有） | — |
| 19 | 保存报**外键失败** `fk_stu_profile_user` | `userId` 是**旧值**（对应用户已不存在） | 见 #20 #21 #22 | #64 #66 #67 |
| 20 | 同上 | 登录成功后还需再调 `getUserInfo` 才写 `localStorage.userId`，失败时被 `catch` **静默吞掉** → 残留旧 id | 登录成功先清 `userId/userRole/userName`；失败改为弹提示；保存失败命中外键类错误时**清本地信息并跳登录页** | #64 #66 |
| 21 | 同上 | `UserCenterView.onMounted` 拿到了最新用户信息，却**只更新 nickname，从未更新 `userId`/`currentUserId`** | onMounted 用最新信息刷新 `currentUserId` + `localStorage.userId` | #66 |
| 22 | `getUserInfo` 返回 **401**（请求头 `Bearer Bearer xxx`） | 后端 `JwtUtil.genToken()` 返回值**自带 `Bearer ` 前缀**，`LoginView` 又硬编码拼了一次 → 后端剥一层后仍带前缀 → JWT 校验失败 → 401 → userId 写不进去 → 外键失败 | LoginView 登录后把 token **归一化**（去掉自带前缀）再存 | #67 |

> 🔑 **这串问题的传播链**：`Bearer Bearer` → 401 → `userId` 未更新（残留旧值）→ 用旧 userId 插入 → 外键失败。
> 中间还叠加了「DB 列类型 / 唯一约束 / 多语句 SQL / 解密方法」4 个独立 Bug。

---

## 四、百宝箱（AI 服务）对接类

| # | 问题现象 | 根本原因 | 修复 | PR |
|---|---|---|---|---|
| 23 | 对话功能不可用（老 AI 服务已退役） | 转发目标指向已删除的自研 AI 服务 | 新增 `TboxAgentService`（Reactor Netty WS 客户端，零新依赖），链路：前端→后端→百宝箱 | #54 |
| 24 | 平台事件流**没有"哪个智能体"的标识**，无法实现"按智能体顺序输出" | 平台把所有节点输出合并为**一条** `TEXT_MESSAGE` 流 | 设计**段标记协议** `<<<AGENT:key>>>`/`<<<END:key>>>` + 后端增量解析器 `AgentMarkerParser` | #55 #56 |
| 25 | 报告流只输出**第 1 段**就结束 | 平台的**系统提示词**决定"单轮只答一段就收尾"，**请求级注入覆盖不了** | 提供《系统提示词·变更指令》，需在百宝箱应用配置中写入并重新发布 | #56 |
| 26 | 报告流一直等到 180s 超时 | `RUN_FINISHED` 未置结束标志 → WS 接收循环永不结束 | `RUN_FINISHED` 时置 `done`（实测 **180s → 15s**） | #55 |
| 27 | 报告落库报 `Invalid bound statement: CareerReportMapper.insert` | 该 Mapper 与 `CareerReportHistoryMapper` **原本零实现**（无 XML 无注解）→ 相关报告接口其实都是坏的 | 补 2 个 XML 映射（15 个方法） | #55 |
| 28 | 平台返回 `RUN_ERROR: Not Open` | 模型网关未开通 / 未重新发布 | 文档记录为阻塞项（平台侧处理） | #53 |

---

## 五、前端体验 / GUI 类

| # | 问题现象 | 根本原因 | 修复 | PR |
|---|---|---|---|---|
| 29 | 前端接口全部指向失效的旧隧道地址 | 国赛时期的两个独立后端外网隧道已失效 | `api.js` 改相对路径 + `vite.config.js` 统一代理 `/api` | #44 |
| 30 | 主界面 emoji 多、观感偏"学生作品" | 设计风格未收敛 | 6 智能体卡片重写 + 全站去 emoji（改用内联 SVG `AppIcon`）+ 收敛渐变/圆角/阴影 | #49 |
| 31 | GUI 点「全部重启」状态看不出变化 | 状态文案格式不变；停止态是灰色不醒目 | 状态加 **PID + 启动于时间**；停止态改红；加「⟳ 处理中…」过渡态与逐个服务进度 | #52 |
| 32 | GUI 日志不清空、切换后端/前端不自动刷新 | 日志以追加模式打开；下拉框无回调 | 启动前清空日志；`OptionMenu` 绑定 `command` 回调自动刷新；新增「清空全部日志」 | #52 #64 |
| 33 | 多智能体页是**假演示**（硬编码 + sleep 模拟） | 当时无真实后端接口 | 重写为真实实现：6 卡片顺序流式 + Markdown 渲染 + 报告名/ID 展示 | #55 |

---

## 六、遗留待办（非本次能解决）

| 项 | 说明 |
|---|---|
| 🔴 **百宝箱模型网关** | 平台侧开通模型；并确认"上架/发布"≠"模型开通" |
| 🔴 **平台系统提示词** | 需把《系统提示词·变更指令》写入应用配置并重新发布，6 段才会完整输出 |
| 🟡 **`TBOX_API_URL` 域名** | 预览态域名会变，后端从环境变量读取，勿硬编码 |
| 🟡 **旧密钥画像数据** | 已清理；重新录入的画像即为当前密钥可解密 |

---

## 七、经验教训（值得固化的规范）

1. **接口契约要单向归一**：token 前缀这类"双方都加"的约定，必须在**存储时归一化**，而不是每个调用点各自判断
2. **DB 列类型要与实体/前端一致**：生成实体时对照 `information_schema` 校验（本次 `Integer` vs `varchar` 导致 500）
3. **逻辑删除字段必须有默认值**：`is_deleted` 为 NULL 会让"保存成功但查不到"
4. **加密字段列长度要按密文规划**：RSA-1024 密文 ≈172 字符，`varchar(50)` 必爆
5. **多语句 SQL 依赖驱动开关**：`allowMultiQueries=true` 必须显式开启
6. **异常不要静默吞掉**：`catch { console.error }` 会掩盖整条故障链（本次 #20）
7. **端口/进程类问题自动化**：提交前检查端口占用、启动前检查依赖服务（`manage.py` 已内置）
8. **导入 SQL 必带字符集参数**：`--default-character-set=utf8mb4`

---

## 八、第二轮自查与修复（代码级复查，2026-09-19）

> 对全文代码逐层复查（迁移完成度 / 结构一致性 / 接口契约），发现并修复以下**残留问题**：

| # | 问题 | 根本原因 | 修复 |
|---|---|---|---|
| 34 | **新环境灌库后仍会复现「保存成功却查不到」** | `数据库结构.sql` 中 `student_profile.is_deleted` 仍是 `varchar(500) NULL DEFAULT NULL`；`manage.py db_seed` 只导结构+数据，**从不执行 003/004 迁移** | 结构脚本改为 `tinyint NOT NULL DEFAULT 0`；`db_seed` 灌库后自动跑幂等迁移 `005`；新增 `python manage.py db migrate` |
| 35 | **新环境灌库后仍会复现 `Data too long`** | 结构脚本里 `expected_salary varchar(50)`、`education varchar(32)` 未同步迁移 003 的加宽 | 结构脚本同步加宽（→ varchar(1000)）；迁移 005 幂等重跑 |
| 36 | **迁移 004 自身会中断** | 004 对 `student_profile_history` / `student_ability_score_history` 写 `SET is_deleted`，但这两张表**没有该列** → ERROR 1054 导致整脚本中止 | 修正 004（只更新确实有该列的表）；新增收敛版 005 |
| 37 | **画像新插入行 `is_deleted` 依旧为 NULL** | `StudentProfileMapper.insert` 从不写 `is_deleted`，DB 默认又是 NULL | insert XML 显式写 `COALESCE(#{isDeleted},0)`；Service 层补默认 0 |
| 38 | **报告/对话里画像字段是 Base64 乱码** | `StudentProfileContextService` 对 education/careerIntentions/jobIntentionDetail/targetCity/expectedSalary/industryPreference/grade **未解密** | 全部改用 `dec()`（RSA 优先，AES 兜底） |
| 39 | **对话上下文里画像同样是乱码** | `AIConversationServiceImpl.addDecryptedField` 用 AES(`decryptFromDB`) 解 RSA 密文 | 抽取 `decryptAny()`：RSA 优先 + AES 兜底 |
| 40 | **#21/#66 的「刷新 userId」修复实际未生效** | `/api/user/getUserInfo` 把 `IV`/`AES` 声明为**必填**，个人中心/多智能体页不传 → 400 被 `catch` 静默吞掉 → userId 从未刷新（外键失败随时复现） | 后端 IV/AES 改 `required=false` 并守卫；前端个人中心补传 IV/AES |
| 41 | **`MatchDetailMapper` 零实现（同 #27 一类）** | 9 个方法无 XML 无注解 → `Invalid bound statement: MatchDetailMapper.batchInsert` | 新增 `MatchDetailMapper.xml`（9 个方法 + resultMap + batchInsert） |
| 42 | **人岗匹配/能力测评/报告仍在调已退役的自研 AI 服务** | `AIServiceImpl`/`JobCompareServiceImpl`/`JobAIAnalysisServiceImpl` 硬编码 `127.0.0.1:8081` 或失效隧道 `*.ofalias.net:50311` | 统一改走 `TboxAgentService.chatSync()`（WS 同步收集）；`WebClientConfig` 去掉退役地址与超长超时；图片对话明确提示暂不支持 |
| 43 | **多智能体报告结束帧无 reportId/reportName** | 后端结束帧只有 `{done,agents,hasMarkers}`，而前端期望 `reportId/reportName`；且落库在 `doOnComplete`（晚于结束帧） | 结束帧到达时即落库并回填 `reportId/reportName/saved`；`saveReport` 改为返回落库信息 |
| 44 | 跨域仅放行 `localhost:5173` | 用 `127.0.0.1:5173` 打开时跨域失败 | CORS 同时放行 `localhost` 与 `127.0.0.1` |
| 45 | `AiConversationMapper.deleteMessagesByConversationId` 用 `@Update` 执行 DELETE | 注解语义错误（可运行但不规范） | 改 `@Delete` |
| 46 | `.env.example` 缺少 `TBOX_REPORT_TIMEOUT_SECONDS` | 变量只在 `application.yml` 有默认值，未在模板声明 | 补上 |
| 47 | **`Cannot add or update a child row: fk_stu_profile_user`（外键失败反复出现）** | 项目 ID 是**雪花算法 64 位**（~2.3e17），超过 JS 安全整数上限（2^53≈9.0e15）。后端把 id 作为 JSON **数字**下发 → 浏览器静默丢精度（实测偏 14）→ 回传的是不存在的 user_id。种子账号低 12 位恰好为 0 所以不暴露，**新注册账号必中** | ① 后端全局 Jackson 把 `Long/long` 序列化为**字符串**（`JacksonConfig`）② 前端所有 ID 保持字符串，去掉 `Number(id)` ③ `getUserInfo` 返回“用户不存在/失效”时清本地身份并跳登录④ 个人资料/意向保存统一走 `handleSaveError` |
| 48 | **岗位列表/星图/岗位管理/岗位对比全部 `网络错误` (500)** | 12 个 `wwy…controller` 均标注 `@CrossOrigin(origins="*")`，与全局 `CorsConfig` 的 `allowCredentials(true)` 冲突 → Spring 在进入 Controller **之前**就抛 `IllegalArgumentException: allowedOrigins cannot contain "*"` | `@CrossOrigin(origins="*")` → `@CrossOrigin`（不指定 origins，与 `org.example.web` 一致，不再冲突） |
| 49 | **验证码“发送失败”但看不出原因；邮件失败还会假成功** | ① 前端读 `res.data.msg`，而后端返回的是 `res.data.message` → 真报错被替换成“发送失败”；② `UserServiceImpl.sendmail/forget_password_sendmail` 标了 `@Async`，SMTP 异常被异步线程吞掉，接口仍返回“已发送” | 前端改读 `message`；去掉 `@Async` 并在 Controller 捕获邮件异常，返回真实原因 |
| 50 | **删库重启后“没库但仍启动”，接口全 500** | `start_service` 忽略 `db_seed()` 返回值；且未找到 mysql 客户端时仅打印一行提示；`_find_mysql` 搜索路径不全 | `db_seed` 返回未就绪即**中止后端启动**；`_find_mysql` 增加 `where`/环境变量/更多安装目录；`db status` 打印客户端、连接错误与表/岗位数量 |

> 🔑 本轮最关键的根因是 **「结构漂移」**：修复只落在 migration，而建表脚本 + 自动灌库链路没有跟上，导致**每次开新环境都会把已修的 Bug 重新引入**。已通过「结构脚本同步 + 灌库后自动跑幂等迁移」根治。

> 🔑 **#47 的排查教训**：页面不再报 `Bearer Bearer` / 401，但外键依然失败，是因为故障链换成了另一根：**JS 64 位精度**。只要 ID 以 JSON 数字跨到浏览器就有丢精度风险，必须改成字符串传输。
