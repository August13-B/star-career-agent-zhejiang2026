# ?????
spring:
  main:
    allow-circular-references: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/youthpath?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: root
    password: 11223344
    hikari:
      connection-test-query: SELECT 1
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8081
  servlet:
    context-path: /api

# OpenAI ??????????
langchain4j:
  open-ai:
    chat-model:
      api-key:
      model-name: qwen3-vl-plus
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      log-requests: true
      log-responses: true
    streaming-chat-model:
      api-key:
      model-name: qwen3-vl-plus
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      log-requests: true
      log-responses: true
    embedding-model:
      api-key: REPLACE_WITH_YOUR_DASHSCOPE_API_KEY
      model-name: text-embedding-v4
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      log-requests: true
      log-responses: true
      max-segments-per-batch: 10
    spring:
      datasource:
        url: jdbc:postgresql://localhost:5432/langchain_rag
        username: postgres
        password: postgres
        driver-class-name: org.postgresql.Driver

    # 向量维度（必须和你的嵌入模型一致）
    vector:
      dimension: 1536 # OpenAI小模型=1536，大模型=3072

  community:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    dev.langchain4j: debug


