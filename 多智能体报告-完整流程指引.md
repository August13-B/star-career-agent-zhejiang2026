# 职业报告多智能体 · 完整流程指引

> 适用：`/multi-agent` 页面跑通"6 智能体顺序流式 → 完整报告 → PDF 导出"
> 更新时间：2026-08-15

---

## 一、前置条件（一次即可）

### 1. 百宝箱应用需按规范更新系统提示词 ⚠️ 关键

当前**请求级注入**只能让模型输出**第 1 段**（平台系统提示词决定"一轮只答一段"）。
请把 `百宝箱/提示词-职业报告多智能体.md` **第六节「系统提示词 · 变更指令」** 粘贴到
百宝箱应用的**系统提示词/人设**配置 → **保存 → 重新发布**。

要点回顾：
```
必须在同一条回复内一次性输出全部 6 个片段，不得中途停止、不得提问；
每段用 <<<AGENT:片段名>>> … <<<END:片段名>>> 包裹（每段结束必须写 END）；
标记之外禁止任何内容；信息不足按「（假设：…）」标注后继续。
```

### 2. 数据库已清理

已执行 `数据库/migrations/002_clean_user_data.sql`：
- 保留：`job_info`(9958) / `job_requirement_profile`(181) / `invitation_code` / `user` 账号
- 清空：学生画像、能力、评分、报告、对话、匹配等**旧密钥**数据

> 之后**新录入**的画像会用当前 `.env` 密钥加密，可正常解密。

### 3. 启动服务

```bat
cd /d "F:\大学\比赛\2026“数字马力杯”第十三届浙江省大学生服务外包创新应用大赛\星职"
python manage.py start all      :: 后端 8080 + 前端 5173（Nginx 未装会跳过）
:: 或 python manage.py gui      :: 图形界面点「⚡一键启动」
```

---

## 二、完整流程（5 步）

### 步骤 1｜登录并**重新录入画像**（必须，新密钥）

1. 打开 **http://localhost:5173** → 登录（或注册新账号，邀请码见下）
2. 进入 **个人中心** → 填写**学生画像**（学历、专业、年级、意向城市、期望薪资、职业意向、技能、证书、项目/实习经历）
3. 触发一次**能力评分**（AI 能力测评页），生成 10 维评分
   - 报告会引用：综合分 + 学历/实习/专业/证书/创新/学习/抗压/沟通/问题解决/团队协作

> 邀请码（管理员/企业/导师注册用）：
> ```sql
> SELECT user_role, invitation_code FROM youthpath.invitation_code;
> -- 2=管理员 3=企业端 4=导师；学生注册一般无需邀请码
> ```

### 步骤 2（可选）｜做一次人岗匹配

在 **职业星图 / 岗位页面** 选定目标岗位做一次匹配 —— 报告会引用"最近一次人岗匹配的目标岗位与得分"，使"职业探索"更贴合。

### 步骤 3｜打开多智能体报告页

访问 **http://localhost:5173/multi-agent**

- 页面显示 6 张智能体卡片（等待中）
- 在「补充说明」里可填**目标岗位**（覆盖画像中的职业意向），例如：`Java 后端开发工程师`

### 步骤 4｜点击「生成职业报告」，观察 6 智能体顺序流式

```
① profile_analysis    画像分析      → 等待 → 生成中(逐字) → 完成
② career_exploration  职业探索      → 等待 → 生成中(逐字) → 完成
③ goal_setting        目标设定      → …
④ path_planning       路径规划      → …
⑤ action_planning     行动计划      → …
⑥ report_composition  报告整合      → … → 全部完成
```

- 每个智能体**实时可见**：当前卡片有打字机效果，前序卡片自动置为"完成"
- 全部完成后：底部出现 **「报告已生成并已保存」** + 报告名 + 报告 ID，并提供 **前往个人中心查看**
- 若未检测到标记：会提示"已按兜底归入报告整合"（说明平台提示词还没改）

### 步骤 5｜查看完整报告 + 导出 PDF

1. 页面底部即为**合并后的完整报告**（一~六章，与项目文档口径一致）
2. **个人中心 → 我的报告** 可查看历史版本
3. **导出 PDF**：`GET /api/career-report/{id}/export/pdf`（服务端 PDFBox + 系统中文字体）
   > 该接口需要登录态，前端以带 token 的请求取回 blob 后下载

---

## 三、验收清单

| # | 检查项 | 期望 |
|---|---|---|
| 1 | 6 段按序出现 | profile_analysis → … → report_composition，无缺段乱序 |
| 2 | 标记外无多余文本 | 无英文开场白、无过渡语（解析器会丢弃并告警） |
| 3 | 逐字流式 | 卡片内容逐字增长 |
| 4 | **画像被引用** | 报告中出现"综合得分/各项评分"与你的意向城市、目标岗位 |
| 5 | 岗位数据不编造 | 薪资/要求来自知识库；未命中时如实说明 |
| 6 | 落库 | `career_report` + `career_report_history` 各 +1 行 |
| 7 | PDF | 中文正常显示、分页正确 |

**验证 SQL**：
```sql
USE youthpath;
SELECT id, report_name, status, JSON_LENGTH(report_content->'$.agents') AS 段数
FROM career_report ORDER BY create_time DESC LIMIT 3;

SELECT report_id, version, change_reason, create_time
FROM career_report_history ORDER BY create_time DESC LIMIT 3;
```

---

## 四、常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 只输出第 1 段 | 平台**系统提示词**未按规范更新（见前置条件 1） |
| 内容里出现"…为加密串，无法解析" | 画像数据是**旧密钥**录的 → 在个人中心**重新录入**（本库已清理，重录即可） |
| `AI 模型网关尚未开通（Not Open）` | 平台模型未开通/未发布 |
| 报告为空或超时 | 检查 `后端/.env` 的 `TBOX_API_URL`；报告超时默认 180s（`TBOX_REPORT_TIMEOUT_SECONDS`） |
| PDF 中文乱码 | 未找到中文字体 → 安装微软雅黑/黑体（`C:/Windows/Fonts/simhei.ttf`） |
| 401 | 未登录/令牌过期 → 重新登录（前端会自动带 `Authorization`） |

---

## 五、相关文件

| 文件 | 说明 |
|---|---|
| `百宝箱/提示词-职业报告多智能体.md` | 6 智能体协议 + **系统提示词变更指令** |
| `数据库/migrations/002_clean_user_data.sql` | 清理旧密钥用户数据（保留业务数据） |
| `后端/.../service/impl/AgentMarkerParser.java` | 段标记增量解析器 |
| `后端/.../service/impl/TboxAgentServiceImpl.java` | 百宝箱 WS 客户端 + 报告流 |
| `后端/.../service/impl/StudentProfileContextService.java` | 账号画像注入（含解密） |
| `后端/.../service/impl/CareerReportPdfService.java` | PDF 导出 |
| `前端/src/views/MultiAgentView.vue` | 6 卡片顺序流式页面 |
